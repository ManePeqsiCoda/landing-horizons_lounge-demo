# Copy to `.env` and fill with your Stripe test publishable key to use the real Card Element.
# Without this key, /reserve runs in prototype demo mode (simulated deposit, no charges).
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
